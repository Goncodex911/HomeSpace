{
  "name": "homespace-workspace",
  "version": "1.0.0",
  "description": "Lumina HomeSpace Fullstack Workspace",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\" \"npm run web --prefix mobile\"",
    "start": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\" \"npm run web --prefix mobile\"",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "mobile": "npm run web --prefix mobile"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongodb": "^7.2.0",
    "mongoose": "^9.6.2"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
